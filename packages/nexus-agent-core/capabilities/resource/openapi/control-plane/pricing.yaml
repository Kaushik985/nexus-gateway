# Generated by `nexus openapi-gen` from control-plane source. DO NOT hand-edit
# structural fields (paths/verbs/types) — they are overwritten on regen. The
# openapi-review skill enriches descriptions/examples/enums in place.
openapi: 3.1.0
info:
    title: Nexus Control Plane Admin API — pricing
    version: dev
    description: 'Generated from control-plane source by openapigen. Structural draft: field names, types, optionality, status codes and IAM tiers are code-derived; enum value sets and required-field rules enforced imperatively in handlers are filled in by the openapi-review skill.'
paths:
    /api/admin/pricing:
        get:
            operationId: listPricing
            summary: List model pricing entries
            description: |-
                Returns all configured per-model pricing records, ordered by effective
                date (most recent first). Each record carries the input and output token
                price per million tokens used for cost estimation of AI Gateway traffic.
            x-nexus-iam-action: iam.ResourceModelPricing.Action(iam.VerbRead)
            x-nexus-tier: auto
            responses:
                "500":
                    description: Internal Server Error
                    content:
                        application/json:
                            schema:
                                type: object
                                additionalProperties: {}
                            example:
                                error:
                                    message: Internal server error
                                    type: server_error
                                    code: ""
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                type: object
                                properties:
                                    data:
                                        type: array
                                        description: All model pricing records, ordered by effective date descending.
                                        items:
                                            type: object
                                            properties:
                                                id:
                                                    type: string
                                                    description: Unique identifier of the pricing record.
                                                modelId:
                                                    type: string
                                                    description: Model identifier the pricing applies to.
                                                inputPricePerMillion:
                                                    type: number
                                                    description: Price per one million input tokens.
                                                outputPricePerMillion:
                                                    type: number
                                                    description: Price per one million output tokens.
                                                effectiveDate:
                                                    description: Date the pricing took effect.
                                additionalProperties: {}
                            example:
                                data:
                                    - id: 9f1c2e3a-4b5d-6e7f-8a9b-0c1d2e3f4a5b
                                      modelId: gpt-4o
                                      inputPricePerMillion: 2.5
                                      outputPricePerMillion: 10
                                      effectiveDate: "2026-01-01T00:00:00Z"
        post:
            operationId: createPricing
            summary: Create or update model pricing
            description: |-
                Creates a pricing record for a model, or updates the existing record when
                one already exists for the same model (upsert keyed on modelId). The input
                and output prices are expressed per one million tokens.
            x-nexus-iam-action: iam.ResourceModelPricing.Action(iam.VerbCreate)
            x-nexus-tier: confirm
            requestBody:
                required: true
                content:
                    application/json:
                        schema:
                            type: object
                            properties:
                                modelId:
                                    type: string
                                    description: Model identifier the pricing applies to. Must be non-empty.
                                inputPricePerMillion:
                                    type: number
                                    description: Price per one million input tokens. Defaults to 0 when omitted.
                                outputPricePerMillion:
                                    type: number
                                    description: Price per one million output tokens. Defaults to 0 when omitted.
                            required:
                                - modelId
                        example:
                            modelId: gpt-4o
                            inputPricePerMillion: 2.5
                            outputPricePerMillion: 10
            responses:
                "400":
                    description: Bad Request — modelId is missing or the body could not be parsed.
                    content:
                        application/json:
                            schema:
                                type: object
                                additionalProperties: {}
                            example:
                                error:
                                    message: modelId is required
                                    type: validation_error
                                    code: ""
                "500":
                    description: Internal Server Error
                    content:
                        application/json:
                            schema:
                                type: object
                                additionalProperties: {}
                            example:
                                error:
                                    message: Failed to create pricing
                                    type: server_error
                                    code: ""
                "201":
                    description: Created
                    content:
                        application/json:
                            schema:
                                type: object
                                properties:
                                    id:
                                        type: string
                                        description: Identifier of the created or updated pricing record.
                                    modelId:
                                        type: string
                                        description: Model identifier the pricing applies to.
                                additionalProperties: {}
                            example:
                                id: 9f1c2e3a-4b5d-6e7f-8a9b-0c1d2e3f4a5b
                                modelId: gpt-4o
    /api/admin/pricing/{id}:
        delete:
            operationId: deletePricing
            summary: Delete model pricing
            description: Deletes the model pricing record identified by the path id. Returns 404 when no record matches the id.
            x-nexus-iam-action: iam.ResourceModelPricing.Action(iam.VerbDelete)
            x-nexus-tier: confirm
            parameters:
                - name: id
                  in: path
                  required: true
                  description: Identifier of the pricing record to delete.
                  schema:
                    type: string
            responses:
                "500":
                    description: Internal Server Error
                    content:
                        application/json:
                            schema:
                                type: object
                                additionalProperties: {}
                            example:
                                error:
                                    message: Failed to delete pricing
                                    type: server_error
                                    code: ""
                "404":
                    description: Not Found — no pricing record matches the given id.
                    content:
                        application/json:
                            schema:
                                type: object
                                additionalProperties: {}
                            example:
                                error:
                                    message: Pricing not found
                                    type: not_found
                                    code: ""
                "204":
                    description: No Content — pricing record deleted successfully.
