# Generated by `nexus openapi-gen` from control-plane source. DO NOT hand-edit
# structural fields (paths/verbs/types) — they are overwritten on regen. The
# openapi-review skill enriches descriptions/examples/enums in place.
openapi: 3.1.0
info:
    title: Nexus Control Plane Admin API — traffic-adapters
    version: dev
    description: 'Generated from control-plane source by openapigen. Structural draft: field names, types, optionality, status codes and IAM tiers are code-derived; enum value sets and required-field rules enforced imperatively in handlers are filled in by the openapi-review skill.'
paths:
    /api/admin/traffic-adapters:
        get:
            operationId: listBuiltinTrafficAdapters
            summary: List built-in traffic adapter IDs
            description: >-
                Returns the sorted list of built-in traffic adapter IDs registered
                from the shared traffic-adapter catalog — the same table the data
                planes (compliance-proxy / agent) register at startup. Admin UIs use
                this list to populate interception-domain forms and other adapter
                pickers, so the catalog cannot drift from runtime registration. IDs
                are returned in ascending lexical order.
            x-nexus-iam-action: iam.ResourceSettings.Action(iam.VerbRead)
            x-nexus-tier: auto
            responses:
                "200":
                    description: Sorted built-in traffic adapter IDs.
                    content:
                        application/json:
                            schema:
                                type: object
                                properties:
                                    data:
                                        type: array
                                        description: Canonical built-in traffic adapter IDs, sorted ascending.
                                        items:
                                            type: string
                                            description: A built-in traffic adapter ID (e.g. an AI provider or web client identifier).
                                required:
                                    - data
                            example:
                                data:
                                    - anthropic
                                    - anthropic-console-web
                                    - azure-openai
                                    - bedrock
                                    - bolt-web
                                    - chatglm-web
                                    - chatgpt-web
                                    - claude-web
                                    - cursor
                                    - gemini
                                    - generic-jsonpath
                                    - github-copilot
                                    - openai-compat
                                    - vertex
                                    - voyage
