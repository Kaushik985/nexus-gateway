# Allowlist for .claude/skills/oss-secret-scan/scan.py — confirmed false-positives.
# One entry per line. `re:` prefix = Python regex matched against the flagged
# value; otherwise a literal substring. `#` starts a comment (whole-line or
# trailing). Add an entry only after triaging and confirming it is NOT a leak.

# Code identifiers / key names / token prefixes (names, not secret values):
nexus_access_token
nxs_scim_
e2e-test-service-token
6595b64144ccf1df

# Documented dev-only fallback (real key comes from env; secrets-env-only rule):
nexus-gateway-default-hmac-secret

# UI form-placeholder example emails (i18n copy, not real addresses):
re:@company\.com
re:@acme\.com
re:@empresa\.com
