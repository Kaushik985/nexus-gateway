// Package configstore provides domain-level helpers for reading and
// writing admin-managed configuration rows (mostly singletons). Each
// file in this package owns a single configuration surface.
package configstore
