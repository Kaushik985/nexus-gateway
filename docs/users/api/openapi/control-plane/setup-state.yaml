# Generated by `nexus openapi-gen` from control-plane source. DO NOT hand-edit
# structural fields (paths/verbs/types) — they are overwritten on regen. The
# openapi-review skill enriches descriptions/examples/enums in place.
openapi: 3.1.0
info:
    title: Nexus Control Plane Admin API — setup-state
    version: dev
    description: 'Generated from control-plane source by openapigen. Structural draft: field names, types, optionality, status codes and IAM tiers are code-derived; enum value sets and required-field rules enforced imperatively in handlers are filled in by the openapi-review skill.'
paths:
    /api/admin/setup-state:
        get:
            operationId: getSetupState
            summary: Read setup wizard completion state
            description: |-
                Returns the current setup wizard completion state, persisted under the
                `setup-wizard-state` system-metadata key. When no state has been stored
                yet, the handler returns the default object `{"completed": false}`.
                Otherwise the stored blob is returned verbatim, so the response is a
                free-form JSON object whose shape is defined by whatever the client last
                wrote via the PUT operation.
            x-nexus-iam-action: iam.ResourceSettings.Action(iam.VerbRead)
            x-nexus-tier: auto
            responses:
                "200":
                    description: 'Current setup wizard state. Either the stored blob, or the `{"completed": false}` default when nothing has been persisted.'
                    content:
                        application/json:
                            schema:
                                type: object
                                description: Free-form setup wizard state blob. The handler does not constrain its keys; it round-trips whatever the client last persisted.
                                additionalProperties: {}
                            example:
                                completed: false
        put:
            operationId: updateSetupState
            summary: Update setup wizard completion state
            description: |-
                Persists the setup wizard completion state to the `setup-wizard-state`
                system-metadata key and emits a settings audit entry. The request body is
                accepted as a free-form JSON value and stored verbatim — the handler
                performs no field-level validation beyond requiring well-formed JSON — and
                the same value is echoed back in the success response.
            x-nexus-iam-action: iam.ResourceSettings.Action(iam.VerbUpdate)
            x-nexus-tier: confirm
            requestBody:
                required: true
                description: Free-form setup wizard state to persist. Stored verbatim; no key is required or validated by the handler. Conventionally carries a `completed` boolean.
                content:
                    application/json:
                        schema:
                            description: Free-form setup wizard state blob. The handler stores whatever well-formed JSON is supplied without constraining its keys.
                        example:
                            completed: true
            responses:
                "200":
                    description: State persisted. Echoes the request body that was stored.
                    content:
                        application/json:
                            schema:
                                description: Echo of the persisted setup wizard state — identical to the request body.
                            example:
                                completed: true
                "400":
                    description: Request body was not well-formed JSON.
                    content:
                        application/json:
                            schema:
                                type: object
                                description: Error envelope produced by the handler's `errJSON` helper.
                                additionalProperties: {}
                            example:
                                error:
                                    message: Invalid body
                                    type: validation_error
                                    code: ""
                "500":
                    description: Persisting the state to system metadata failed.
                    content:
                        application/json:
                            schema:
                                type: object
                                description: Error envelope produced by the handler's `errJSON` helper.
                                additionalProperties: {}
                            example:
                                error:
                                    message: Failed to save setup state
                                    type: server_error
                                    code: ""
