# Generated by `nexus openapi-gen` from control-plane source. DO NOT hand-edit
# structural fields (paths/verbs/types) — they are overwritten on regen. The
# openapi-review skill enriches descriptions/examples/enums in place.
openapi: 3.1.0
info:
    title: Nexus Control Plane Admin API — exemption-requests
    version: dev
    description: 'Generated from control-plane source by openapigen. Structural draft: field names, types, optionality, status codes and IAM tiers are code-derived; enum value sets and required-field rules enforced imperatively in handlers are filled in by the openapi-review skill.'
paths:
    /api/admin/exemption-requests:
        post:
            operationId: createRequest
            summary: Submit a compliance exemption request
            description: >-
                Employee-facing endpoint to submit a request that compliance
                interception be temporarily lifted for a specific transaction.
                The request is recorded with status PENDING and later approved
                or rejected by a reviewer. The handler validates that
                transactionId, sourceIp, targetHost, and reason are all present
                and non-empty; durationMinutes defaults to 240 when omitted and
                requestedBy defaults to "employee" when omitted. On success the
                persisted request row is returned.
            x-nexus-iam-action: iam.ResourceComplianceExemption.Action(iam.VerbUpdate)
            x-nexus-tier: confirm
            requestBody:
                required: true
                content:
                    application/json:
                        schema:
                            type: object
                            properties:
                                transactionId:
                                    type: string
                                    description: Identifier of the traffic transaction the exemption applies to.
                                sourceIp:
                                    type: string
                                    description: Source IP address of the request to be exempted.
                                targetHost:
                                    type: string
                                    description: Target host the exemption applies to.
                                reason:
                                    type: string
                                    description: Human-readable justification for the exemption.
                                durationMinutes:
                                    type: integer
                                    description: Requested exemption duration in minutes. Defaults to 240 when omitted.
                                requestedBy:
                                    type: string
                                    description: Identity of the requester. Defaults to "employee" when omitted.
                            required:
                                - transactionId
                                - sourceIp
                                - targetHost
                                - reason
                        example:
                            transactionId: txn-7f3a91c2
                            sourceIp: 10.4.2.17
                            targetHost: api.openai.com
                            reason: Temporary access needed to validate an integration build.
                            durationMinutes: 120
                            requestedBy: jdoe
            responses:
                "400":
                    description: Bad Request — body could not be parsed, or one of transactionId, sourceIp, targetHost, reason is missing or empty.
                    content:
                        application/json:
                            schema:
                                type: object
                                additionalProperties: {}
                            example:
                                error:
                                    message: transactionId, sourceIp, targetHost, and reason are required
                                    type: validation_error
                                    code: ""
                "500":
                    description: Internal Server Error — the request could not be persisted.
                    content:
                        application/json:
                            schema:
                                type: object
                                additionalProperties: {}
                            example:
                                error:
                                    message: Internal server error
                                    type: server_error
                                    code: ""
                "201":
                    description: Created — the exemption request was recorded with status PENDING.
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/store_ExemptionRequest'
                            example:
                                id: 9b1c0a44-2e7d-4c8a-b6f0-3a1e5d2c9b88
                                transactionId: txn-7f3a91c2
                                sourceIp: 10.4.2.17
                                targetHost: api.openai.com
                                reason: Temporary access needed to validate an integration build.
                                status: PENDING
                                durationMinutes: 120
                                reviewedBy: null
                                reviewNote: null
                                reviewedAt: null
                                createdAt: "2026-01-15T09:30:00Z"
                                requestedBy: jdoe
components:
    schemas:
        store_ExemptionRequest:
            type: object
            description: A persisted compliance exemption request row.
            properties:
                id:
                    type: string
                    description: Server-generated unique identifier of the request.
                transactionId:
                    type: string
                    description: Identifier of the traffic transaction the exemption applies to.
                sourceIp:
                    type: string
                    description: Source IP address of the exempted request.
                targetHost:
                    type: string
                    description: Target host the exemption applies to.
                reason:
                    type: string
                    description: Justification supplied by the requester.
                status:
                    type: string
                    description: Lifecycle state of the request. Set to PENDING on creation; transitions to APPROVED or REJECTED on review.
                    enum:
                        - PENDING
                        - APPROVED
                        - REJECTED
                durationMinutes:
                    type: integer
                    description: Exemption duration in minutes.
                reviewedBy:
                    type:
                        - string
                        - "null"
                    description: Identifier of the reviewer who approved or rejected the request; null while PENDING.
                reviewNote:
                    type:
                        - string
                        - "null"
                    description: Optional note recorded by the reviewer; null while PENDING.
                reviewedAt:
                    type:
                        - string
                        - "null"
                    format: date-time
                    description: Timestamp of review; null while PENDING.
                createdAt:
                    type: string
                    format: date-time
                    description: Timestamp the request was created.
                requestedBy:
                    type: string
                    description: Identity of the requester.
            required:
                - id
                - transactionId
                - sourceIp
                - targetHost
                - reason
                - status
                - durationMinutes
                - createdAt
                - requestedBy
