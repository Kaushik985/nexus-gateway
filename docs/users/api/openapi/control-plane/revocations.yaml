# Generated by `nexus openapi-gen` from control-plane source. DO NOT hand-edit
# structural fields (paths/verbs/types) — they are overwritten on regen. The
# openapi-review skill enriches descriptions/examples/enums in place.
openapi: 3.1.0
info:
    title: Nexus Control Plane Admin API — revocations
    version: dev
    description: 'Generated from control-plane source by openapigen. Structural draft: field names, types, optionality, status codes and IAM tiers are code-derived; enum value sets and required-field rules enforced imperatively in handlers are filled in by the openapi-review skill.'
paths:
    /api/admin/revocations:
        get:
            operationId: listRevocations
            summary: List token revocation events for replay
            description: >-
                Returns revoked-token records with id greater than `since`, in ascending
                id order, so a resource-server-side checker can catch up on revocations it
                missed while disconnected from the message queue. Each event mirrors the
                live MQ revocation event shape, and the response carries a `lastId` cursor
                the caller pipelines into the next request's `since`. Returns 503 when the
                revocation store is not configured.
            x-nexus-iam-action: iam.ResourceRevocation.Action(iam.VerbRead)
            x-nexus-tier: auto
            parameters:
                - name: since
                  in: query
                  required: false
                  description: >-
                    Return only events with row id strictly greater than this cursor.
                    Parsed as a non-negative integer; empty, malformed, or negative
                    values are treated as 0 (replay from the beginning).
                  schema:
                    type: integer
                    format: int64
                    minimum: 0
                    default: 0
                - name: limit
                  in: query
                  required: false
                  description: >-
                    Maximum number of events to return. Parsed as an integer and clamped
                    to the range [1, 1000]; empty or malformed values fall back to the
                    default of 500.
                  schema:
                    type: integer
                    minimum: 1
                    maximum: 1000
                    default: 500
            responses:
                "503":
                    description: Service Unavailable — the revocation store is not configured.
                    content:
                        application/json:
                            schema:
                                type: object
                                additionalProperties: {}
                            example:
                                error: revocation store not configured
                                type: server_error
                                code: REVOCATION_STORE_UNAVAILABLE
                "500":
                    description: Internal Server Error — the revocation store query failed.
                    content:
                        application/json:
                            schema:
                                type: object
                                additionalProperties: {}
                            example:
                                error: failed to list revocations
                                type: server_error
                                code: ""
                "200":
                    description: OK — revocation events and the next-page cursor.
                    content:
                        application/json:
                            schema:
                                type: object
                                additionalProperties: {}
                                properties:
                                    events:
                                        type: array
                                        description: Revocation events with row id greater than `since`, ascending.
                                        items:
                                            type: object
                                            properties:
                                                event_id:
                                                    type: string
                                                    description: Synthetic replay event id, formatted as `evt_replay_<rowId>`.
                                                revoked_at:
                                                    type: string
                                                    format: date-time
                                                    description: Time the revocation was recorded.
                                                expires_at:
                                                    type: string
                                                    format: date-time
                                                    description: Time after which the revocation no longer needs to be enforced.
                                                scope:
                                                    type: string
                                                    description: >-
                                                        Revocation scope. Source: revocation.Scope constants in
                                                        packages/control-plane/internal/identity/authserver/revocation/event.go.
                                                    enum:
                                                        - jti
                                                        - user
                                                        - device
                                                        - session
                                                target_jti:
                                                    type: string
                                                    description: Target token JTI (present for scope `jti`).
                                                target_user_id:
                                                    type: string
                                                    description: Target user id (present for scope `user`).
                                                target_device_id:
                                                    type: string
                                                    description: Target device id (present for scope `device`).
                                                target_session_id:
                                                    type: string
                                                    description: Target session id (present for scope `session`).
                                                reason:
                                                    type: string
                                                    description: Free-text reason recorded with the revocation.
                                    lastId:
                                        type: integer
                                        format: int64
                                        description: >-
                                            Cursor of the highest row id in this page; pass as `since` on the
                                            next request to continue replay.
                            example:
                                events:
                                    - event_id: evt_replay_42
                                      revoked_at: "2026-05-29T12:00:00Z"
                                      expires_at: "2026-06-29T12:00:00Z"
                                      scope: device
                                      target_device_id: dev_abc123
                                      reason: unenroll
                                lastId: 42
