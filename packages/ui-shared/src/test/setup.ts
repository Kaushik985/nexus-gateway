// Global Vitest setup for @nexus-gateway/ui-shared. Imports jest-dom's
// vitest-aware matchers so any `*.test.tsx` in this package can use
// `toBeInTheDocument`, `toBeDisabled`, etc. without restating the
// import.
import '@testing-library/jest-dom/vitest';
